// @ts-check (VSCode, para activar chequeo de tipos básico)

/**
 * @typedef {import ('../models/task')} Task
 * @typedef {import ('../models/goal')} Goal
 */

/** @type {Task[]} */
const tasks = [];

/** @type {Goal[]} */
const goals = [];

module.exports = {
    tasks,
    goals
};